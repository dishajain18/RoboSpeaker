This code is written specific to Windows cmd 
If you want to use the same code you can use it for other OS by changing the command string in the code
for example:
for Mac OS
command = f"say {x}" 
here say is the command for Mac OS
